# Optional foreground Bird compatibility only. Prefer an existing browser
# session. Codex Reset Request managed services reject environment-only X
# credentials and never embed these values in service definitions.
export AUTH_TOKEN=""
export CT0=""
export GUEST_ID=""
export TWID=""
export LANG="en"
